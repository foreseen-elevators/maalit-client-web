# Base URL of the existing Maalit backend (first-http-node-coolify).
# All API calls happen client-side, so this must be NEXT_PUBLIC_-prefixed.
# Falls back to the production URL below if unset, so local dev works
# out of the box against the real live backend.
NEXT_PUBLIC_API_BASE_URL=https://first-app.055449264.xyz
